---
id: git
name: Git Workflow
triggers: [git, commit, stash, branch, diff]
priority: 10
---
Write clear, descriptive git commit messages. Verify modified/untracked files using `git status` and inspect changes with `git diff` before making commits. Always restore any stashed changes carefully.
